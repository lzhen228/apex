spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://47.94.120.15:5432/apex_platform
    username: scrm_user
    password: liao1996Z..

  data:
    redis:
      host: 47.94.120.15
      port: 6379
      database: 0
      password:
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

aliyun:
  oss:
    endpoint: https://oss-cn-shanghai.aliyuncs.com
    access-key-id: ${OSS_ACCESS_KEY_ID:your-access-key-id}
    access-key-secret: ${OSS_ACCESS_KEY_SECRET:your-access-key-secret}
    bucket-name: ${OSS_BUCKET_NAME:your-bucket-name}

jwt:
  secret: ${JWT_SECRET:apex-platform-secret-key-for-jwt-token-generation-very-long-random-string}
  expiration: 86400000

logging:
  level:
    org.springframework.security: DEBUG
